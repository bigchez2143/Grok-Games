import {dealerTools,instructions} from '@/lib/dealer-tools';
import {json} from '@/lib/table-store';
export function GET(){return json({name:'After Hours Blackjack API',version:'1.0.0',transport:'Stateless Streamable HTTP MCP; POST /api/mcp/{dealerToken}',supportedMcpVersions:['2025-03-26','2025-06-18','2025-11-25'],authentication:'The 64-character secret at the end of the MCP URL grants access only to that table’s dealer. HTTP API uses Authorization: Bearer TOKEN. Create/replace a key from the game’s connection dialog; replacing it revokes the previous key.',http:{read:{method:'GET',path:'/api/dealer'},call:{method:'POST',path:'/api/dealer',body:{tool:'post_chat',arguments:{message:'Welcome to the table!',expectedRevision:3}}}},tools:dealerTools,instructions,concurrency:'Every mutation requires expectedRevision from the latest state. A conflicting HTTP mutation returns 409. MCP tool failures return isError:true. Read get_table again before retrying. Retried stale actions cannot draw twice.',privacy:'Only the face-up dealer card is visible during the player phase. The deck, future cards, player credential, and dealer credential are excluded from dealer state. The player’s recent table chat is visible to the connected bot.',limitations:'Grok tools run during a Grok conversation turn. In-game messages do not wake Grok or establish a background worker. The user says Continue in Grok to resume. Hosting must allow Grok to reach this endpoint without a website login redirect.',sources:['https://docs.x.ai/grok/connectors','https://docs.x.ai/grok/connectors/custom-mcp-tunneling','https://modelcontextprotocol.io/specification/2025-11-25/basic/transports']});}
